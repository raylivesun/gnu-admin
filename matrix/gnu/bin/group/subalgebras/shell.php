<?php
    date_format($date); // date format string
    date_timezone_set('UTC'); // set the timezone string
    date_timezone_set('Brazil/Portuguese'); // set the timezone string
    echo date_format($date); // date format string

    // set the timezone string
    $window = date_format_set('UTC'); // set the timezone string
    echo date_format($window); // date format string
    echo '<br/>'; // set the timezone string

    // popup the popup window
    $popup = window.opendir(PHP_AMQP_MAX_CHANNELS); // open the popup window
    echo date_format($popup); // date format string
    echo '<br/>'; // set the timezone string

    // static function to display the popup window
    $static = window.opendir(PHP_AMQP_MAX_CHANNELS); // open the static function window
    echo date_format($static); // date format string
    echo '<br/>'; // set the timezone string

    // dynamic function to display the popup window
    $dynamic = window.opendir(PHP_AMQP_MAX_CHANNELS); // open the dynamic function window
    echo date_format($dynamic); // date format string
    echo '<br/>'; // set the timezone string
?>
    

    