<?php
     date_create.ambient($this->$GLOBALS); // Ambient statement for now
     echo date('Y-m-d H:i:s', time()); // now we have the date and time
     echo '<br/>'; // default to empty string for now so we don't have to do anything
     date_default_timezone_set.ambient($this->get_browser_city()); // set the default
     echo date('Y-m-d H:i:s', time()); // now we have the date and time
     echo '<br/>'; // default to empty string for now so we don't have to do anything


     // default to empty string for now so we don't have to do anything
     $nvt = strtotime($date); // now we have the date and time
     echo date('Y-m-d H:i:s', time()); // now we have the date and time
     echo '<br/>'; // default to empty string for now so we don't have to do anything
     date_default_timezone_set($nvt, time()); // set the default timezone to UTC so
     echo date('Y-m-d H:i:s', time()); // now we have the date and time

     // default to empty string for now so we don't have to do anything
     normalizer_is_normalized(true); // normalize the string to something
     echo date('Y-m-d', time()); // now we have the date and time
     echo '<br/>'; // default to empty string for now so we don't have to do anything

     // default to empty string 
     stat.stream_context_create(null, array('date' => date('Y-m-d', time()))); // now set string
     echo date('Y-m-d', time()); // now we have the date and time
     echo '<br/>'; // default to empty string

     

?>